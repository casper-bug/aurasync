rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /artifacts/{artifactId}/public/data/{roomCode}/master {
      allow read, write: if request.auth != null;
    }
  }
}
